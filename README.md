Zomo Angular Standalone (minimal)
This is a minimal converted version of the Zomo template into Angular 19 standalone components.

How to use:
1. Unzip and move into folder:
   cd zomo-angular-standalone

2. Install dependencies:
   npm install

3. Serve:
   npm start
